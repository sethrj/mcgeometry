#import os

Import('env')
e = env.Clone()

sourceFiles = Glob('*.cpp')
#headerFiles = Glob('*.hpp')

#Compile all the source files together into a library
e.StaticLibrary(target = 'mcgeometry', source = sourceFiles)

#compile unit tests
SConscript('tests/SConscript')
