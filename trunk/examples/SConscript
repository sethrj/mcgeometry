import re

Import('env')
Import('useLibrary')
e = env.Clone()

useLibrary(e, 'transupport')
useLibrary(e, 'mcgeometry')

Import('outputDirPath')

e.Program(outputDirPath + 'meshTiming', 'meshTiming.cpp')
e.Program(outputDirPath + 'meshComparison', ['meshComparison.cpp','extra/mtrand.cc'])
